# ── Secrets / environment (NEVER commit) ─────────────────────────────
.env
.env.*
!.env.example
!.env.*.example
**/.env
**/.env.local
*.pem
*.key

# ── Java / Maven (backend build output) ──────────────────────────────
target/
*.class
*.jar
*.war

# ── Node / Vite (frontend deps & build output) ───────────────────────
node_modules/
dist/
dist-ssr/
*.local

# ── Logs ─────────────────────────────────────────────────────────────
logs/
*.log
npm-debug.log*

# ── IDE / OS ─────────────────────────────────────────────────────────
.idea/
*.iml
.vscode/*
!.vscode/extensions.json
.DS_Store
Thumbs.db

# ── Local uploads (runtime-generated resumes, etc.) ──────────────────
uploads/
