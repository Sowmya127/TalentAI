{
  "_comment": "Template for the AWS Secrets Manager secret that holds ONLY the sensitive values. Fill in real values when creating the secret (see create-secret.sh) - do NOT commit real values. Each key here is referenced individually from the ECS task definition via valueFrom '<secret-arn>:<KEY>::'.",

  "DB_USERNAME": "talentai_app",
  "DB_PASSWORD": "REPLACE_WITH_REAL_DB_PASSWORD",
  "JWT_SECRET": "REPLACE_WITH_A_FRESH_256_BIT_KEY_MIN_32_CHARS",

  "_optional_when_enabled": "Uncomment these in the real secret only when mail/AI are actually wired up.",
  "MAIL_USERNAME": "",
  "MAIL_PASSWORD": "",
  "OPENAI_API_KEY": ""
}
